<style global>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;
  ol {
    @apply list-inside;
    @apply list-decimal;
  }
  ul {
    @apply list-inside;
    @apply list-disc;
  }
</style>
